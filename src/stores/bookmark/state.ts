import { ref } from 'vue';
import type { bookmarkInterface } from './interface';


export const currentBookmark = ref({} as bookmarkInterface);
// export const requestResponsePending = ref(false as boolean);
