import { ref } from 'vue';

export const id = ref('' as string);
export const name = ref('' as string);
export const mail = ref('' as string);
export const registration = ref(new Date as Date);
export const last_connection = ref(new Date as Date);
export const image = ref('' as string);
export const experience = ref(0 as number);
export const gold = ref(0 as number);
export const gem = ref(0 as number);